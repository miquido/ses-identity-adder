name: SES identity adder
repos:
  gitlab: miquido/terraform/ses-identity-adder
badges: [ ]
related: [ ]
description: |-
  Adds lambda that enables to easily add identity to AWS
usage: |-

  ```hcl
    module "ses-identity-adder" {
      source      = "git::https://github.com/miquido/ses-identity-adder"
      environment = "example"
      project     = "example"
    }
  ```

include:
  - "docs/terraform.md"

# License of this project
license: "MIT"
